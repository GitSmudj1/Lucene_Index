<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<doc>
  <docno>
    <textblock>FR940224-0-00047</textblock>
  </docno>
  <parent>
    <textblock>FR940224-0-00006</textblock>
  </parent>
  <text>
    <textblock>5. FP controls apply to Iran and Syria for computers controlled by 4A03A or 4A94F (i.e., computers with a CTP of</textblock>
    <textblock>6 Mtops or greater). See §785.4(d)(1) of this subchapter.</textblock>
    <textblock>* * * * *</textblock>
    <textblock>11. In Category 4 (Computers), following Advisory Note 7, the Technical Note under the heading ``Information on How to Calculate Composite Theoretical Performance (CTP)'' is revised to read as follows:</textblock>
    <textblock>Information on How to Calculate ``Composite Theoretical Performance'' (``CTP''):</textblock>
    <textblock>Technical Note:</textblock>
    <textblock>``Composite theoretical performance'' (CTP).</textblock>
    <textblock>Abbreviations used in this Technical Note</textblock>
    <textblock>CE ``computing element'' (typically an arithmetic logical unit)</textblock>
    <textblock>FP floating point</textblock>
    <textblock>XP fixed point</textblock>
    <textblock>t execution time</textblock>
    <textblock>XOR exclusive OR</textblock>
    <textblock>CPU central processing unit</textblock>
    <textblock>TP theoretical performance (of a single CE)</textblock>
    <textblock>CTP ``composite theoretical performance'' (multiple CEs)</textblock>
    <textblock>R effective calculating rate</textblock>
    <textblock>WL word length</textblock>
    <textblock>L word length</textblock>
    <textblock>* multiply</textblock>
    <textblock>Execution time `t' is expressed in microseconds, TP and ``CTP'' are expressed in Mtops (millions of theoretical operations per second) and WL is expressed in bits.</textblock>
    <textblock>Outline of ``CTP'' Calculation Method</textblock>
    <textblock>``CTP'' is a measure of computational performance given in millions of theoretical operations per second (Mtops). In calculating the ``Composite Theoretical Performance'' (``CTP'') of an aggregation of ``Computing Elements'' (``CEs''), the following three steps are required:</textblock>
    <textblock>1. Calculate the effective calculating rate (R) for</textblock>
    <textblock>each ``computing element'' (``CE'');</textblock>
    <textblock>2. Apply the word length adjustment (L) to the effective calculating rate (R), resulting in a Theoretical Performance (TP) for each ``computing element'' (``CE'');</textblock>
    <textblock>3. If there is more than one ``computing element'' (``CE''), combine the Theoretical Performances (TPs), resulting in a ``Composite Theoretical Performance'' (``CTP'') for the aggregation.</textblock>
    <textblock>Details for these steps are given in the following section.</textblock>
    <textblock>Note 1:</textblock>
    <textblock>For aggregations of multiple ``computing elements'' (``CEs'') that have both shared and unshared memory subsystems, the calculation of ``CTP'' is completed hierarchically, in two steps: first, aggregate the groups of ``computing elements'' (``CEs'') sharing memory; second calculate the ``CTP'' of the groups using the calculation method for multiple ``computing elements'' (``CEs'') not sharing memory.</textblock>
    <textblock>Note 2:</textblock>
    <textblock>``Computing elements'' (``CEs'') that are limited to input/output and peripheral functions (e.g., disk drive, communication and video display controllers) are not aggregated into the ``CTP'' calculation.</textblock>
  </text>
</doc>
